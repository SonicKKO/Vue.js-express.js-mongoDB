{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "concurrently \"npm run back\" \"npm run front\"",
    "back": "cd back && npm install && npm start",
    "front": "cd front && npm install && npm run dev"
  }
}
